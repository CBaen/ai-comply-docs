

---

## Verified Capabilities Added By Prior Instances (2026-04-23 — Sextant session)

**Google Search Console API via local CLI.** `python C:/Users/baenb/.claude/scripts/gsc.py` exposes Google Search Console as a CLI. Subcommands: `list-sites`, `summary <site> [--days N]`, `queries <site> [--days N] [--limit N]`, `pages <site> [--days N] [--limit N]`, `sitemaps <site>`, `submit-sitemap <site> <url>`, `inspect <site> <url>`. Multi-site capable — any GSC property verified under the authenticated Google account is queryable. Auth via `gsc-auth.py` (one-time OAuth, full `webmasters` scope). Token at `C:/Users/baenb/.claude/.gsc-token.json` auto-refreshes. The `inspect` subcommand uses URL Inspection API and acts as a re-discovery signal that bumps URLs in Google's priority crawl queue. Built during Sextant session for aicompliancedocuments.com after GL needed to know real impressions/clicks/CTR. Reusable for any future site GL adds to the same Google account.

**Stripe MCP creates LIVE products + prices via API (HANDOFF previously said test-mode only — wrong).** Run `mcp__plugin_stripe_stripe__authenticate`, give GL the OAuth URL, GL approves, then `mcp__plugin_stripe_stripe__create_product` + `mcp__plugin_stripe_stripe__create_price` work in live mode. Prices are in cents (`unit_amount: 29900` = $299). Products return `livemode: true`. Sextant session created 4 live products + prices in ~30 seconds total. Use this BEFORE offering to coach GL through the Stripe dashboard.

**Direct primary-source verification beats spawning more research agents.** When two browser-Claude briefs (or any two secondary sources) disagree on specifics — section numbers, vote tallies, dollar amounts — use `WebFetch` directly on the primary source (enrolled bill at capitol.texas.gov, statute page at leg.colorado.gov, regulation text at cppa.ca.gov, etc.) and ask for "exact section number + quoted text." A third research agent will give you a third wrong answer because it's reading the same wrong secondary sources. Pattern proven during Sextant Texas TRAIGA section number reconciliation: site had 552.102, brief 1 said 552.107, brief 2 said 552.110 — enrolled bill confirmed 552.102.

**Auto-commit hook is local-only — `git push origin main` is required for Vercel to deploy.** The post-write hook auto-commits but does NOT push. Vercel deploys from GitHub, not local commits. After ANY user-visible change, the verification chain is: (1) `git push origin main`, (2) `vercel ls --yes` shows recent Ready deploy, (3) `curl -s <production-url>` and grep for expected content (use file-write+grep for minified single-line HTML), (4) only then say "live." This is what CLAUDE.md anti-pattern #1 (Reporting without watching) looks like in this repo specifically.
